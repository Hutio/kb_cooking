# Cooking Vault — 작성 규약

`Cooking/` 은 Obsidian 볼트다. 이 파일이 볼트 전체의 **유일한 포맷 표준**이다.
문서를 새로 쓰거나 고칠 때는 반드시 이 규약을 따른다.

## 0. 원칙

- **한국어로 쓴다.** 원어(영문/한자)는 `aliases` 와 제목 옆 괄호에만 둔다.
- **재현 가능해야 한다.** "적당히", "취향껏" 금지. 무게(g)·부피(ml)·시간(분)·온도(°C)·불세기를 명시한다.
- **판단 기준을 쓴다.** "볶는다"가 아니라 "가장자리가 갈색이 돌 때까지 3분 볶는다".
- **왜를 쓴다.** 팁은 현상만이 아니라 이유(화학·물리)를 한 문장이라도 붙인다.
- **표로 쓴다.** 3항목 이상이 같은 속성으로 비교되면 산문이 아니라 표다. (§4-A)
- **검증하고 쓴다.** 한국어 자료에는 반증된 통설이 그대로 돌아다닌다. (§0-A)
- **링크는 실재하는 문서만.** 없는 문서를 `[[ ]]` 로 가리키지 않는다. 필요하면 문서를 먼저 만든다.

## 0-A. 사실 검증 — 이 볼트의 신뢰도가 걸린 항목

### 영어로 조사한다

**한국어 검색 결과를 1차 근거로 삼지 않는다.** 특히 서양·중식·일식 재료는 한국어 자료에 오역과 출처 불명의 수치가 대량으로 복제돼 있다. 영어로 검색하고, 원산지 규정은 원문을 확인한다.

| 신뢰도 | 출처 |
|---|---|
| 1차 | USDA FoodData Central · FSIS · FDA · EFSA · 식약처 · 농촌진흥청 · 학술 논문 |
| 1차 (규격) | EU DOP/PDO·IGP, 프랑스 AOC, 이탈리아 Consorzio 공식 문서 (파르미지아노·프로슈토·모짜렐라 등) |
| 2차 (신뢰) | Serious Eats / J. Kenji López-Alt, Cook's Illustrated·America's Test Kitchen, Harold McGee 『On Food and Cooking』, Modernist Cuisine, ChefSteps |
| **불가** | 개인 블로그, 커뮤니티 글, 출처 없는 요리 콘텐츠, 한국어 위키 계열 |

수치(온도·기간·함량·비율)는 **최소 2개 출처에서 교차 확인**한다. 확인 안 되면 쓰지 않는다. 범위로 쓰되 그 범위의 근거가 있어야 한다.

### 반증된 통설 — 그대로 옮기지 말 것

아래는 한국어 자료에 흔하지만 사실이 아니다. 문서에 등장하면 **적극적으로 바로잡는다.**

| 통설 | 사실 |
|---|---|
| 겉을 지지면 육즙이 갇힌다(sear to seal) | 반증됨. 시어링한 고기가 수분을 더 잃는다. 시어링의 목적은 마이야르 반응으로 인한 풍미다 |
| 고기에서 나오는 붉은 물은 피다 | 미오글로빈 용액이다. 피는 도축 시 거의 제거된다 |
| MSG는 몸에 해롭다 / 중국음식점 증후군 | 근거 없음. FDA는 GRAS로 분류한다 |
| 스테이크는 한 번만 뒤집어야 한다 | 자주 뒤집는 쪽이 더 균일하고 빠르게 익는다 |
| 굽기 전 실온에 1시간 두면 속까지 온도가 오른다 | 2cm 스테이크가 1시간에 몇 °C 오를 뿐이다. 표면 건조 효과가 실익이다 |
| 파스타 삶는 물에 기름을 넣으면 안 붙는다 | 기름은 물 위에 뜬다. 효과 없다 |
| 알루미늄 조리도구가 치매를 유발한다 | 근거 없음 |
| 전자레인지가 영양을 파괴한다 | 가열 시간이 짧아 오히려 손실이 적은 편이다 |
| 냉동하면 영양이 파괴된다 | 대부분 유지된다. 손상되는 것은 조직과 식감이다 |
| 천일염이 정제염보다 건강하다 | 미네랄 차이는 영양학적으로 무의미한 수준이다. 차이는 입자 크기와 용해 속도다 |
| 꿀·조청이 설탕보다 건강하다 | 당류로서 대사는 유사하다. 차이는 향·수분·환원당 여부다 |
| 갈변한 감자는 싹만 도려내면 된다 | 녹색 부위·쓴맛·광범위 발아는 폐기 기준이다 |
| 양념에 오래 재우면 속까지 밴다 | 소금·설탕 외 대부분은 표면 수 mm에 머문다 |

이 표는 예시지 전부가 아니다. **"흔히 알려진 것과 다르다"는 정보를 만나면 그것이 문서의 가장 가치 있는 부분**이므로 근거와 함께 명시한다.

## 0-B. 정본(定本) 원칙 — 수치는 한 곳에만 쓴다

같은 사실을 여러 문서가 각자 조사해 쓰면 값이 갈린다. 실제로 감사에서 12건이 나왔다 —
스테이크 웰던 온도가 4문서 4스케일, 브라인 농도가 3문서 3값, 올리브유 발연점이 정반대.

**주제별로 정본 문서를 하나 정하고, 다른 문서는 그 값을 반복하지 않는다.**

| 주제 | 정본 문서 |
|---|---|
| 육류 익힘·안전 온도 | [[육류 (개요)]] |
| 단백질 변성·콜라겐 젤라틴화 온도 | [[육류 (개요)]] |
| 유지 발연점·산패 | [[조미료 (개요)]] |
| 염지·브라인 농도와 시간 | [[염지]] |
| 발효 온도·염도 | [[발효]] |
| 국물 추출 온도·시간 | [[육수]] |
| 마이야르·캐러멜화 온도대 | [[마이야르 반응]] · [[캐러멜화]] |
| 튀김 온도 | [[튀기기]] |
| 전분 호화·젤라틴화 | [[전분]] · [[젤라틴]] |

### 정본이 아닌 문서에서 수치를 쓸 때

- **한 개의 대표값만** 쓰고 정본 문서로 링크한다. 범위·조건·예외는 정본에만 둔다.
- 예: 닭고기 문서는 "중심부 74°C([[육류 (개요)]] 참조)"까지. 시간-온도 살균 곡선은 정본에만.
- 정본과 다른 값을 쓰고 싶으면 **정본을 먼저 고친다.** 문서마다 다른 값이 존재해서는 안 된다.
- 정본 문서로의 링크는 §4-B의 "지금 열어봐야 이해되는 경우" 예외에 해당하므로 남긴다.

### 값이 갈릴 때

출처 등급(§0-A)이 높은 쪽을 따른다. 기관 권고와 실무 관행이 다르면 **둘 다 쓰되 무엇이 어느 쪽인지 명시**한다.
예: 돼지고기 덩어리 — USDA 63°C+3분 / 식약처 75°C 1분. 어느 하나를 지우지 말고 근거와 함께 병기한다.

## 1. 디렉터리 구조

```
Cooking/
├── 홈.md                                  # 최상위 MOC
├── 레시피/
│   ├── 한식/{반찬,메인요리,김치,전,국,찌개,면,떡,찜,음료}/
│   ├── 양식/{파스타,피자,샐러드,수프,계란요리,샌드위치,스테이크,리조또}/
│   ├── 중식/{면,볶음,튀김,메인요리}/
│   ├── 일식/{덮밥,면,스시,튀김}/
│   ├── 베이킹/{빵,케이크,쿠키,파이}/
│   ├── 소스/{한식,양식,중식,아시아}/
│   └── {베트남,태국,인도}/
├── 재료/
│   ├── 식재료/{육류,해산물,채소,조미료,유제품,곡물,콩견과,과일,베이킹,기타}/
│   └── 향신료/{한식,양식,아시아,허브}/
├── 이론/{맛,조리기법}/
└── 도구/{칼,조리,전자,주방}/
```

각 상위 카테고리 폴더에는 `<카테고리> (개요).md` 인덱스 문서를 둔다.
하위 폴더는 항목이 5개를 넘으면 인덱스를 만든다.

## 2. 파일명

- 한국어 문서명. 영문 파일명(`Korean.md`, `Spices.md` 등) 금지.
- 인덱스 문서는 `XXX (개요).md`.
- 볼트 전체에서 **파일명은 유일해야 한다** (링크를 파일명으로만 걸기 때문).

## 3. Frontmatter

모든 문서는 YAML frontmatter 로 시작한다. 공통 필드:

```yaml
type:        # recipe | ingredient | technique | taste | tool | index  (필수)
title:       # 문서 제목 (필수, 파일명과 동일)
aliases:     # [영문명, 이명]  — 없으면 생략
tags:        # [] (필수, 최소 2개)
updated:     # YYYY-MM-DD (필수)
```

### type 별 추가 필드

**recipe**
```yaml
cuisine:          # 한식 | 양식 | 중식 | 일식 | 동남아 | 인도 | 베이킹 | 소스
category:         # 찌개, 반찬, 파스타, 피자 …
servings:         # 숫자 (인분)
time_total:       # 숫자 (분, 총 소요)
time_active:      # 숫자 (분, 실제 손 가는 시간)
difficulty:       # 쉬움 | 보통 | 어려움
key_ingredients:  # [핵심 재료 3~5개]
techniques:       # [[[볶기]] 등 이론/조리기법 문서명]
```

**ingredient**
```yaml
group:          # 육류 | 해산물 | 채소 | 버섯 | 곡물 | 유제품 | 향신료 | 허브 | 조미료 | 기타
season:         # 제철 (예: 9~11월) — 해당 없으면 생략
storage:        # 실온 | 냉장 | 냉동 | 실온·냉장
storage_short:  # 단기 보관 요약 1줄 (예: 냉장 5~7일, 키친타월+밀폐)
storage_long:   # 장기 보관 요약 1줄 (예: 냉동 6개월, 1회분 소분·밀봉)
tastes:         # [감칠맛, 단맛] — 이론/맛 문서명과 일치
pairs_with:     # [궁합 좋은 재료 3~6개]
```

**technique**
```yaml
group:      # 기본 | 응용 | 발효
heat:       # 건열 | 습열 | 복합 | 무가열
temp_range: # 예: 160~180°C — 해당 없으면 생략
```

**taste**
```yaml
group:      # 기본 | 복합 | 조합
compounds:  # [글루탐산, 이노신산] — 조합 맛이면 생략
```

**tool**
```yaml
group:      # 칼 | 조리 | 전자 | 주방
materials:  # [스테인리스, 주철, 논스틱]
```

**index**
```yaml
scope:      # 이 인덱스가 덮는 범위 (예: 한식 레시피 전체)
```

## 4-A. 표 우선 원칙

**같은 속성으로 3개 이상을 비교하면 표로 쓴다.** 불릿 나열은 비교가 안 되고 스캔이 안 된다.

산문은 **원리를 설명할 때만** 쓴다 — 왜 그런 온도인지, 어떤 반응이 일어나는지. 항목 나열·비교·수치는 전부 표다.

### 재료 문서 표준 표

**고르기 — 등급/품종 표**
```
| 등급·품종 | 기준 | 특징 | 적합한 용도 |
```

**손질과 형태 — 형태별 표**
```
| 형태 | 크기·두께 | 향·식감 | 용도 |
```

**보관 — 보관 표 (단기·장기 각각)**
```
| 방법 | 온도 | 기간 | 포장·전처리 | 주의 |
```

**맛과 성분 — 성분 표**
```
| 성분 | 함량·수치 | 역할 | 가열 시 변화 |
```

**조리 활용 — 용도 표**
```
| 용도 | 형태·양 | 투입 시점 | 대표 요리 |
```

### 규칙

- 표 앞에는 한 문장으로 무엇을 비교하는 표인지 쓴다. 표 뒤에는 표로 표현 못 하는 원리만 덧붙인다.
- 열은 4~7개. 그 이상은 쪼갠다. 셀 안에 문장을 넣지 말고 구·수치로 쓴다.
- 폐기 신호, 안전 경고, 반증된 통설 교정은 표 밖에 굵게 쓴다 — 놓치면 안 되는 내용이기 때문이다.
- 표만으로 채우지 않는다. 문서당 **표 3개 이상 + 원리 설명 산문**이 균형이다.

## 4-B. 링크 절제 원칙

**관계는 frontmatter property가, 탐색은 `## 관련 문서`가 담당한다.** 본문 링크는 최소로 남긴다.

링크가 많을수록 좋은 게 아니다. 문단마다 파란 글씨가 박히면 읽기 힘들고, Obsidian 그래프도 의미 없는 엣지로 뒤덮인다.

### 어디에 링크를 두는가

| 위치 | 규칙 |
|---|---|
| frontmatter `tastes` `pairs_with` `techniques` `key_ingredients` | **위키링크로 쓴다.** 그래프 엣지는 여기서 나온다 |
| `## 관련 문서` | 링크 유지. 실제 탐색 지점이다 |
| 개요(index) 문서의 목록 | 링크 유지. 목록이 곧 탐색이다 |
| 그 외 본문·표 | **원칙적으로 평문.** 아래 예외만 링크 |

### frontmatter 링크 표기

대상 문서가 **실제로 존재할 때만** 링크로 쓴다. 없으면 평문.

```yaml
tastes: ["[[감칠맛]]", "[[짠맛]]"]
pairs_with: ["[[마늘]]", "[[양파]]", "[[후추]]", 레몬]
```

### 본문에서 링크를 남기는 예외

- 그 문서를 **지금 열어봐야 이 문장이 이해되는** 경우 (원리·기법 문서로의 첫 참조)
- 같은 문서로는 **문서당 한 번만.** 두 번째부터는 평문
- frontmatter에 이미 있는 이름은 본문에서 링크하지 않는다
- 표 셀 안에는 링크를 넣지 않는다 (개요 문서의 목록 표는 예외)

### 목표 밀도

재료 문서 기준 본문(관련 문서 제외) 링크 **5개 이하**. 개요 문서는 제한 없음.

## 4. 본문 섹션

제목은 `# 제목 (원어)` 한 줄, 그 다음 `> 한두 문장 요약` (blockquote).
그 아래는 type 별 고정 섹션 순서를 지킨다. **(선택)** 표시가 없는 섹션은 필수.

### recipe
```
## 재료
### 주재료          # 계량 + 손질 상태 병기: `신김치 300g (1/4포기, 2주 이상 발효)`
### 양념            # (선택) 양념이 별도로 묶이는 경우
### 육수            # (선택)
## 만드는 법        # 번호 목록. 각 단계에 시간·불세기·완료 판단 기준
## 핵심 포인트      # **굵은 소제목**: 설명 형식. 3~7개. 이유를 함께.
## 응용 변형        # (선택) 인분 조절, 재료 대체, 매운맛 조절 등
## 실패 구출        # (선택) 짤 때 / 싱거울 때 / 퍽퍽할 때
## 보관과 재가열    # (선택)
## 유래             # (선택)
## 관련 문서        # `- [[문서명]] — 한 줄 설명`
```

### ingredient
```
## 고르기            # 좋은 것 고르는 기준, 등급·품종·산지 차이, 피해야 할 상태
## 손질과 형태        # 다듬기·써는 법·형태별(통/편/채/다짐) 용도 차이
## 보관              # ★ 볼트의 핵심 섹션. 아래 두 하위 섹션 필수.
### 단기 보관         # 며칠~2주. 온도대·용기·전처리·기대 수명·상함의 신호
### 장기 보관         # 냉동·건조·절임·발효·오일침지 등. 방법 / 기간 / 해동·복원법 / 품질 손실
## 맛과 성분          # 맛 성분, 향 화합물, 영양, 가열 시 변화
## 조리 활용
## 핵심 포인트        # (선택)
## 관련 문서
```

**보관 섹션 작성 규칙**
- 온도를 숫자로: "냉장"이 아니라 "냉장 0~4°C", "냉동 -18°C 이하".
- 기간을 숫자로: "오래 간다" 금지 → "냉장 5~7일 / 냉동 6개월".
- 방법을 재현 가능하게: 용기, 포장재, 소분 단위, 전처리(데치기·물기 제거 등)를 적는다.
- **왜 그런지** 한 문장: 에틸렌 가스, 냉해, 수분 이동, 산패, 냉동 화상 등.
- **상함의 신호**: 색·냄새·점액·곰팡이 등 버려야 할 기준을 명시한다.
- 장기 보관은 **복원까지** 쓴다: 해동 방법, 해동 후 용도 제한(예: 냉동 후엔 생식 불가).

### technique
```
## 원리            # 무슨 일이 일어나는가 (열전달, 단백질 변성, 마이야르 등)
## 기본 방법
## 변수            # 온도·시간·도구에 따른 결과 차이
## 흔한 실수
## 적용 요리
## 관련 문서
```

### taste
```
## 원리
## 주요 급원
## 조리 활용
## 다른 맛과의 관계
## 관련 문서
```

### tool
```
## 종류와 선택
## 사용법
## 관리와 보관
## 핵심 포인트      # (선택)
## 관련 문서
```

### index
```
## 시작하기        # 이 카테고리를 처음 보는 사람의 학습 순서
## 전체 목록       # 하위 분류별 링크 목록
## 관련 문서
```

## 5. 금지 사항

- `## Tags` 섹션, 본문 끝의 맨 `#태그` 줄 → **frontmatter `tags` 로만** 쓴다.
- 경로형 링크 `[[맛/기본/감칠맛]]` → `[[감칠맛]]` 처럼 파일명만.
- 이모지 헤딩(`## 🔥 …`) → 인덱스 문서의 목록 항목에서만 허용.
- 존재하지 않는 문서로의 링크.
- 빈 섹션, "추후 작성" 플레이스홀더.

## 6. 검증

작업 후 반드시 실행한다.

```bash
python3 scripts/validate.py
```

frontmatter 누락·필수 필드 누락·깨진 링크·금지 섹션·중복 파일명을 검사한다.

## 7. git

원격: `https://github.com/Hutio/kb_cooking.git` · 기본 브랜치 `main`

**작업 단위로 커밋한다.** 에이전트 웨이브 하나, 정리 패스 하나가 한 커밋이다.
파일 300개를 건드리는 전역 변환 전에는 반드시 커밋해서 되돌릴 지점을 만든다.

커밋 메시지는 한국어로, 제목 한 줄 + 본문에 **무엇을 왜 바꿨는지**. 수치가 바뀌었으면 전후를 적는다.

`.gitignore` 로 제외하는 것: `.DS_Store`, iCloud 충돌 사본(`* [0-9].md`),
`.obsidian/workspace.json`(기기별 상태), `.claude/settings.local.json`(개인 설정).
`.obsidian/` 의 나머지 설정(그래프·단축키·테마)은 볼트 경험의 일부라 공유한다.

**푸시도 커밋과 함께 자동으로 한다.** 사용자가 승인했다(2026-08-29). 되돌릴 일이 생기면
`git revert` 로 처리하고, 원격 히스토리를 다시 쓰는 force push 는 하지 않는다.
